{% extends "layouts.app" %}

{% block content %}

    <div class="bg-light ">
    <div class="container-fluid ">

        <div class="row  pt-sm-5 pb-5">
            <div class="col-12">
                <a class="btn btn-link pl-0 pb-0 small d-sm-none" data-toggle="collapse" href="#collapseMenu" role="button" aria-expanded="false" aria-controls="collapseMenu">
                    <i class="fa fa-bars" aria-hidden="true"></i>
                </a>
            </div>
            <div class="col-sm-4 col-md-3 pr-sm-5 mb-5 collapse dont-collapse-sm" id="collapseMenu">
                <div class="card mb-3">
                    <div class="card-body pb-3">
                        <h6 class="card-subtitle mb-0 text-muted">{{ __('Menu') }}</h6>
                    </div>

                    <ul class="list-group list-group-flush">
                        <li class="list-group-item {{ active(['account.edit_profile.index'], 'list-group-item-primary') }}">
                            <a href="{{ route('account.getNews') }}" class="active">News</a>
                        </li>
                        <li class="list-group-item {{ active(['account.edit_profile.index'], 'list-group-item-primary') }}">
                            <a href="{{ route('account.getCategory') }}" class="active">Category</a>
                        </li>
                        <li class="list-group-item {{ active(['account.edit_profile.index'], 'list-group-item-primary') }}">
                            <a href="{{ route('account.getProductCategory') }}" class="active">Product Category</a>
                        </li>
                        <li class="list-group-item {{ active(['account.edit_profile.index'], 'list-group-item-primary') }}">
                            <a href="{{ route('account.getProduct') }}" class="active">Product</a>
                        </li>
                         <li class="list-group-item {{ active(['account.edit_profile.index'], 'list-group-item-primary') }}">
                            <a href="{{ route('account.getProductPrice') }}" class="active">Product Price</a>
                        </li>
                        


                       
                    </ul>
                </div>

            </div>
            <div class="col-sm-8 col-md-9">
                {% block user_area %}{% endblock %}


            </div>
        </div>
    </div>
    </div>

{% endblock %}
